//
//  main.cpp
//  Algorithms C++
//
//  Created by Taniusa :* on 07.04.2021.
//

#include <iostream>
#include "count_even_numbers.h"
#include "sum_positiv_elem_in_odd_pos.hpp"
#include "bank_deposit.hpp"

int main (){
//    int list[10] = {2,4,3,5,7,9};
//    int list_size = sizeof(list) / sizeof(list[0]);
//
//    std:: cout << evenNumbers(list, list_size) << "\n";
//
//    std:: cout << sum (5, 10) << "\n";
    
    
//// ***** calculate the sum of numbers from an array that are non negative and on the odd positions
//    int list[10] = {1,0,5,-7,9,3};
//    int list_size = sizeof(list) / sizeof(list[0]);
//
//    int sum = sum_positiv_elem_in_odd_pos(list, list_size);
//    std::cout << "sum of elements = " << sum << "\n";

// ****** The deposit in the bank is x euro. It increases annually by p percent. Each year, the amount of the deposit becomes larger. It is necessary to determine in how many years the contribution will be at least y euro.
//    int x = 500;
//    int y = 1000;
//    int p = 10;
//    
//    std::cout << bank_deposit(x, y, p) << "\n";
}

